# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SigTuple' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SigTuple
  pod 'SDWebImage'
  pod 'Toast-Swift'
  target 'SigTupleTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SigTupleUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
